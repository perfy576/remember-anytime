interface:
  display_name: "Remember Anytime"
  short_description: "Persist and enforce project rules and reusable workflows."
  default_prompt: "Use $remember-anytime to initialize, obey, or solidify persistent project workflows."
