# Default Rule Template

These are the generic template sections written by `scripts/init_remember_anytime.py` when no source file is provided.

Replace the placeholders with project-specific rules. Do not rely on the template itself as a source of truth.

Prefer splitting larger durable knowledge into `server.md`, `platforms.md`, and `ui.md` so future agents can match rules to the current workflow.
